
import {
  createStyle,
} from 'src/styles';

const styles = createStyle({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  text: {
    flex: 1,
    textAlign: 'center',
    fontSize: 20,
  },
});

export default styles;
